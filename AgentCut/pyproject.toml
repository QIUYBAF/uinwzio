[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentcut"
version = "1.1.0.dev0"
description = "Agent-native semantic video editing runtime remastered for low-friction Codex and ChatGPT Work use."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
render = ["numpy>=1.26", "Pillow>=10.0"]
api = ["numpy>=1.26", "Pillow>=10.0", "fastapi>=0.110", "uvicorn>=0.27"]
dev = ["numpy>=1.26", "Pillow>=10.0", "pytest>=8.0", "jsonschema>=4.0", "fastapi>=0.110", "httpx>=0.27"]

[project.scripts]
agentcut = "agentcut.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["agentcut*"]

[tool.setuptools.package-data]
"agentcut.data.libraries" = ["*.json"]
